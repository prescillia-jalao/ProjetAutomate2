{
    "Liste des analyses" : [
        {%  for analyse in liste %}
        {
            "Id" : "{{ analyse.id }}",
            "Nom" : "{{ analyse.nom }}",
            "Valeur inférieure" : "{{ analyse.val_inf }}",
            "Valeur supérieure" : "{{ analyse.val_sup }}"
        }
        {% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}