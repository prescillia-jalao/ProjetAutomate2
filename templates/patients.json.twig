{
    "Liste des patients" : [
        {%  for patient in liste %}
        {
            "id" : "{{ patient.id }}",
            "nom" : "{{ patient.nom }}",
            "prenom" : "{{ patient.prenom }}",
            "age" : "{{ patient.age }}"
        }
        {% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}